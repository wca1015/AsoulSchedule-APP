# Add project specific R8 rules here.
# AGP will combine all keep rule files in src/main/keepRules to pass to R8
#
# For more details, see
#   https://d.android.com/r/tools/r8/keep-rules

# If your project uses WebView with JS, uncomment the following
# and specify the fully qualified class name to the JavaScript interface
# class:
#-keepclassmembers class fqcn.of.javascript.interface.for.webview {
#   public *;
#}

# ===== P6：kotlinx-serialization 官方推荐 keep 规则（release 开了 R8 收缩） =====
-keepattributes *Annotation*, InnerClasses
-dontnote kotlinx.serialization.AnnotationsKt

# kotlinx.serialization.json 内部 Companion（Json 实例等）
-keepclassmembers class kotlinx.serialization.json.** {
    *** Companion;
}
-keepclasseswithmembers class kotlinx.serialization.json.** {
    kotlinx.serialization.KSerializer serializer(...);
}

# 项目 DTO 的生成序列化器与 Companion，防止被 R8 裁剪
-keep,includedescriptorclasses class com.example.asoul.**$$serializer { *; }
-keepclassmembers class com.example.asoul.** {
    *** Companion;
}
-keepclasseswithmembers class com.example.asoul.** {
    kotlinx.serialization.KSerializer serializer(...);
}
